.BoardFields {
    position: relative;
    overflow: hidden;

    &__buttons {
        display: flex;
        justify-content: flex-end;
        padding-right: 10px;

        & > * {
            margin: 8px 4px;
        }
    }
}
